---
- name: Autoremove obsolete Debian kernels and packages
  ansible.builtin.apt:
    autoremove: true
    purge: true
  when: uip_remediate_boot_cleanup | bool
  tags:
    - uip_remediate
    - uip_remediate_debian
    - uip_remediate_kernel
    - uip_preupgrade
