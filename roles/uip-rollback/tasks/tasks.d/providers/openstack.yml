---
- name: Fail with explicit OPENSTACK provider rollback procedure
  ansible.builtin.fail:
    msg: >-
      Provider-side rollback is enabled for openstack, but destructive restore
      must be implemented through approved enterprise API workflow. Snapshot:
      {{ uip_rollback_snapshot_state.provider_snapshot_ref }}
  tags:
    - uip_rollback
